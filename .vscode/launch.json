{
  "configurations": [
    

    {
      "type": "chrome",
      "name": "http://src/│├── main.jsx                  ← Vite entry point, mounts App├── App.jsx                   ← Root: owns modal state, composes all sections│├── styles/│   ├── variables.css         ← All CSS custom properties (colours, fonts)│   └── global.css            ← Resets, keyframe animations, scrollbar│├── constants/│   └── data.js               ← All static content (nav links, stats, steps,│                                plans, testimonials, FAQs) — one source of truth│├── hooks/│   └── useScrolled.js        ← Custom hook: detects scroll past threshold│├── components/               ← Reusable UI, used across multiple sections│   ├── LogoMark.jsx          ← SVG logo icon (used by Nav + Footer)│   ├── Nav.jsx               ← Sticky nav with scroll effect + CTA button│   └── Footer.jsx            ← Legal links + branding bar│├── sections/                 ← Full-width page sections, top to bottom│   ├── HeroSection.jsx       ← Headline, SPF stats, dual CTAs│   ├── SPFCalculator.jsx     ← Interactive 4-input diagnostic form + live result│   ├── HowItWorks.jsx        ← 4-step sidebar + animated detail panel│   ├── PricingSection.jsx    ← 3 tiers, annual toggle, per-plan modal│   ├── Testimonials.jsx      ← Auto-rotating carousel + dot nav│   ├── FAQ.jsx               ← Accordion expand/collapse│   └── CTASection.jsx        ← Email capture with validation + success state│└── modals/    └── DemoModal.jsx         ← \"Check my heat pump\" overlay with 3-field form",
      "request": "launch",
      "url": "http://src/│├── main.jsx                  ← Vite entry point, mounts App├── App.jsx                   ← Root: owns modal state, composes all sections│├── styles/│   ├── variables.css         ← All CSS custom properties (colours, fonts)│   └── global.css            ← Resets, keyframe animations, scrollbar│├── constants/│   └── data.js               ← All static content (nav links, stats, steps,│                                plans, testimonials, FAQs) — one source of truth│├── hooks/│   └── useScrolled.js        ← Custom hook: detects scroll past threshold│├── components/               ← Reusable UI, used across multiple sections│   ├── LogoMark.jsx          ← SVG logo icon (used by Nav + Footer)│   ├── Nav.jsx               ← Sticky nav with scroll effect + CTA button│   └── Footer.jsx            ← Legal links + branding bar│├── sections/                 ← Full-width page sections, top to bottom│   ├── HeroSection.jsx       ← Headline, SPF stats, dual CTAs│   ├── SPFCalculator.jsx     ← Interactive 4-input diagnostic form + live result│   ├── HowItWorks.jsx        ← 4-step sidebar + animated detail panel│   ├── PricingSection.jsx    ← 3 tiers, annual toggle, per-plan modal│   ├── Testimonials.jsx      ← Auto-rotating carousel + dot nav│   ├── FAQ.jsx               ← Accordion expand/collapse│   └── CTASection.jsx        ← Email capture with validation + success state│└── modals/    └── DemoModal.jsx         ← \"Check my heat pump\" overlay with 3-field form"
    }
  ]
}
